# Recommended: use GTM as the main container. Leave direct GA4 blank if GA4 is configured inside GTM to avoid duplicate events.
VITE_GTM_ID=GTM-XXXXXXX
VITE_GA4_ID=
VITE_META_PIXEL_ID=
VITE_TIKTOK_PIXEL_ID=
VITE_GOOGLE_ADS_ID=
# Optional POST endpoint for CRM / Google Apps Script / webhook
VITE_LEAD_ENDPOINT=
